import styled from 'styled-components';

const StyledPieChart = styled.div`
  position: relative;
  width: 150px;
  height: 150px;

  svg {
    margin: 25px;
    position: absolute;
  }
`;

export default StyledPieChart;
