import styled from 'styled-components';

export const StyledForm = styled.form`
  input {
    display: block;
    margin: 5px;
    border-radius: 3px;
  }

  input[type=submit] {
    cursor: pointer;
  }
`;
